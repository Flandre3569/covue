const <%= props.name %> = () => import('./<%= props.name %>.vue')
export default {
  path: '/<%= props.lowerName %>',
  name: '<%= props.firstUpperName %>',
  component: <%= props.name %>,
  children: [
  ]
}